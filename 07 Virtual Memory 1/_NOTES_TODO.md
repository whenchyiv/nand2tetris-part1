# Hack VM Parser Project 1

## TODO

- [ ] Fix handling of arg1 and arg2 properties on the Parser class. Currently cannot be used to check if e.g. the command has no arg2 because it will throw an error. Need to first check if the tokens even exist.
- [x] strip out blank lines from the file
- [x] add comment handling
